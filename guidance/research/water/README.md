# Water / housing moratorium — city research

**Primary path:** upload studies at **`/admin/research`** (category **Water & permits**). Paste a digest with study names, dates, URLs, and citable numbers — generate reads the digest, not the PDF binary.

This folder is the **legacy git path**. `water-rebuttal.md` still loads when water/housing-supply storylines match. You can mirror admin digests here for version control, or rely on uploads in local SQLite.

Replace placeholder bullets in `water-rebuttal.md` with verified facts from official city documents before citing in generated issues.
